import styled from "styled-components";

export const TemplateList = styled.ul`
	display: flex;
	flex-wrap: wrap;
	padding: 0 60px;
	list-style: none;
	margin: 16px 0 56px;
	overflow-x: hidden;
	overflow-y: auto;
`;
