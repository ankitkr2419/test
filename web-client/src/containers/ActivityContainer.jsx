import React from "react";

const ActivityContainer = (props) => {
	return (
		<>
			<h4 className="text-center text-uppercase">Activity Log</h4>
		</>
	);
};

ActivityContainer.propTypes = {};

export default ActivityContainer;
